[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "omnicounci1l-judge"
version = "2.0.0"
description = "AI judge engine for OmniCouncil — optional external AI judgment for consensus validation"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "omnicounci1l-core>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
